function SectionTitle({ title }: { title: string }) {
  return (
    <div className="flex justify-center">
      <h2 className="font-bold text-28">{title}</h2>
    </div>
  );
}

export default SectionTitle;
